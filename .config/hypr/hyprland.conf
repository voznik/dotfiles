# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

debug {
  disable_logs = false
}

$terminal = ghostty
$mainMod = SUPER

# See https://wiki.hyprland.org/Configuring/Monitors/
source = ~/.config/hypr/monitors.conf
# See https://github.com/ash-17/omarchy-external-monitor/blob/main/README.md
# exec-once = ~/.config/hypr/scripts/monitor_watcher.sh

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

## Hypr Ecosystem

# Core components (authentication, lock screen, notification daemon)
# exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = hypridle
exec-once = hyprpm reload

# Execute your favorite apps at launch
# https://github.com/savedra1/clipse?tab=readme-ov-file#hyprland
exec-once = clipse -listen # run listener on startup
exec-once = copyq -listen # run listener on startup
# Add networkmanager applet to tray in waybar
exec-once = nm-applet --indicator


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,ua
    kb_variant =
    kb_model =
    kb_options = caps:menu,grp:win_space_toggle
    kb_rules =
    numlock_by_default= true
    follow_mouse = 1

    touchpad {
        natural_scroll = false
        tap-to-click = true
        disable_while_typing = true
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 4
    gaps_out = 4
    border_size = 1
    col.active_border = rgba(33ccffee) rgba(8f00ffee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 2
    blur {
       enabled = true
       size = 4
       passes = 2
       new_optimizations = yes
       ignore_opacity=true
       special = true
       popups = true
    }

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }
}

animations {
    enabled = true

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_status = master
}

# https://wiki.hypr.land/Configuring/Gestures/
gesture = 3, horizontal, workspace

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    vfr = 1
    vrr = 1
    enable_swallow = false
    swallow_regex = (foot|kitty|allacritty|Alacritty|ghostty|wezterm)
}

# Overview
plugin {
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(000000)
        workspace_method = first 1 # [center/first] [workspace] e.g. first 1 or center m+1

        enable_gesture = false # laptop touchpad, 4 fingers
        gesture_distance = 300 # how far is the "max"
        gesture_positive = false
    }
}


# Source a file (multi-file configs)
source = ~/.config/hypr/workspaces.conf

# ####### FROM https://github.com/end-4/dots-hyprland/blob/main/dots/.config/hypr/hyprland/rules.conf

# ######## Workspace rules ########
workspace = special:special, gapsout:30

# ######## Layer rules ########
layerrule = match:namespace .*, xray on
# layerrule = match:namespace .*, no_anim on
layerrule = match:namespace walker, no_anim on
layerrule = match:namespace selection, no_anim on
layerrule = match:namespace overview, no_anim on
layerrule = match:namespace anyrun, no_anim on
layerrule = match:namespace indicator.*, no_anim on
layerrule = match:namespace osk, no_anim on
layerrule = match:namespace hyprpicker, no_anim on

layerrule = match:namespace noanim, no_anim on
layerrule = match:namespace gtk-layer-shell, blur on
layerrule = match:namespace gtk-layer-shell, ignore_alpha 0
layerrule = match:namespace launcher, blur on
layerrule = match:namespace launcher, ignore_alpha 0.5
layerrule = match:namespace notifications, blur on
layerrule = match:namespace notifications, ignore_alpha 0.69
layerrule = match:namespace logout_dialog # wlogout, blur on

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

# -- Fixed Enpass Rules --
windowrule {
    name = enpass-rule
    match:title = ^(Enpass)$
    
    float = true
    center = true
    stay_focused = true
    opacity = 1 override
}

# -- qt-sudo --
windowrule {
    name = qt-sudo-rule
    match:title = qt-sudo
    float = true
}

# -- tab Rules --
windowrule {
    name = copyq-rule
    match:class = com.github.hluk.copyq
    
    float = true
    center = true
    size = 620 840
    opacity = 1 override
    stay_focused = true
}

# -- Calamares --
windowrule {
    name = calamares-rule
    match:class = ^(calamares)$
    float = true
}

# -- Walker --
windowrule {
    name = walker-rule
    match:title = walker
    # stayfocused = true
}

# -- Clipse --
windowrule {
    name = clipse-rule
    match:title = (clipse)
    
    float = true
    center = true
    size = 620 400
    opacity = 1 override
}

# Picture-in-Picture
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, float on
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, keep_aspect_ratio on
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, move (monitor_w*.73) (monitor_h*.72)
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, size (monitor_w*.25) (monitor_h*.25)
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, float on
windowrule = match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$, pin on

# Floating
windowrule = match:title ^(Open File)(.*)$, center on
windowrule = match:title ^(Open File)(.*)$, float on
windowrule = match:title ^(Select a File)(.*)$, center on
windowrule = match:title ^(Select a File)(.*)$, float on
windowrule = match:title ^(Choose wallpaper)(.*)$, center on
windowrule = match:title ^(Choose wallpaper)(.*)$, float on
windowrule = match:title ^(Choose wallpaper)(.*)$, size (monitor_w*.60) (monitor_h*.65)
windowrule = match:title ^(Open Folder)(.*)$, center on
windowrule = match:title ^(Open Folder)(.*)$, float on
windowrule = match:title ^(Save As)(.*)$, center on
windowrule = match:title ^(Save As)(.*)$, float on
windowrule = match:title ^(Library)(.*)$, center on
windowrule = match:title ^(Library)(.*)$, float on
windowrule = match:title ^(File Upload)(.*)$, center on
windowrule = match:title ^(File Upload)(.*)$, float on
windowrule = match:title ^(.*)(wants to save)$, center on
windowrule = match:title ^(.*)(wants to save)$, float on
windowrule = match:title ^(.*)(wants to open)$, center on
windowrule = match:title ^(.*)(wants to open)$, float on

windowrule = match:class ^(steam_app).*, immediate on

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, killactive,
# bind = $mainMod, Q, exec, ~/.config/hypr/scripts/close_window.sh
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod, T, exec, $terminal
bind = $mainMod, E, exec, /opt/enpass/Enpass showassistant
bind = $mainMod, L, exec, nwgbar
# bind = $mainMod, N, exec, thunar
bind = $mainMod SHIFT, 65, togglefloating,
bindr=SUPER, SUPER_L, exec, walker
bind = $mainMod SHIFT, A, exec, nwg-drawer -mb 10 -mr 10 -ml 10 -mt 10
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod SHIFT, P, togglesplit, # dwindle
bind = $mainMod, 36, exec, $terminal  # 36 = Enter
bind = $mainMod, M, exec, nwg-displays
bind = SUPER, Tab, hyprexpo:expo, toggle

# Mainmod + Function keys
bind = $mainMod, F1, exec, ghostty
bind = $mainMod, F2, exec, brave
bind = $mainMod, F3, exec, thunar
bind = $mainMod, F4, exec, doublecmd
bind = $mainMod, F5, exec, /usr/bin/octopi
bind = $mainMod, F6, exec, gparted
bind = $mainMod, F7, exec, $HOME/Programs/Enpass/EnpassPortable
bind = $mainMod ALT SHIFT, left, movecurrentworkspacetomonitor, -1
bind = $mainMod ALT SHIFT, right, movecurrentworkspacetomonitor, +1
bind = $mainMod SHIFT, F8, exec, ~/.config/hypr/scripts/monitor_switch.sh
bind = $mainMod, F9, exec, meld
bind = $mainMod, F10, exec, flameshot gui
bind = $mainMod, F11, exec, snapper-tools
bind = $mainMod, F12, exec, garuda-rani

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, H, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, L, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, K, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move to workspace with focused container with ALT + SHIFT + [0-9]
bind = ALT SHIFT, 1, movetoworkspace, 1
bind = ALT SHIFT, 2, movetoworkspace, 2
bind = ALT SHIFT, 3, movetoworkspace, 3
bind = ALT SHIFT, 4, movetoworkspace, 4
bind = ALT SHIFT, 5, movetoworkspace, 5
bind = ALT SHIFT, 6, movetoworkspace, 6
bind = ALT SHIFT, 7, movetoworkspace, 7
bind = ALT SHIFT, 8, movetoworkspace, 8
bind = ALT SHIFT, 9, movetoworkspace, 9
bind = ALT SHIFT, 0, movetoworkspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bind = $mainMod, , workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
# bindm = $mainMod, mouse:272, movewindow
# bindm = $mainMod, mouse:273, resizewindow

# background
exec-once = wpaperd

# status bar
exec-once = waybar
layerrule2 = blur , waybar
layerrule2 = ignorezero , waybar

# set volume (laptops only and may or may not work on PCs)
bind = ,122, exec, pamixer --decrease 5; notify-send " Volume: "$(pamixer --get-volume) -t 500
bind = ,123, exec, pamixer --increase 5; notify-send " Volume: "$(pamixer --get-volume) -t 500
bind = ,121, exec, pamixer --toggle-mute; notify-send " Volume: Toggle-mute" -t 500
bind = ,XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle; notify-send "System Mic: Toggle-mute" -t 500

# other bindings
bind = $mainMod, M, fullscreen, 1
bind = $mainMod, F, fullscreen, 0
bind = $mainMod SHIFT, F, fullscreenstate, 0 2
bind = ,232,exec,brightnessctl -c backlight set 5%-
bind = ,233,exec,brightnessctl -c backlight set +5%
bind = $mainMod SHIFT,C, exec, killall -9 wpaperd && wpaperd
bind = $mainMod SHIFT,W, exec, killall -9 waybar && waybar >/dev/null 2>&1 &
bind = $mainMod SHIFT,E, exec, ~/.config/hypr/scripts/restart_walker.sh
bind = $mainMod, B, exec, buku -p -f 3 | sed 's/\\t/ /g' | walker --dmenu --placeholder 'Search Bookmarks...' | cut -d ' ' -f 1 | xargs --no-run-if-empty buku -o

# Screenshots:
#
# Change to swappy https://github.com/jtheoof/swappy
# Old functions only commented out to make it easier for the user to change if necessary.

# Snip a selection and pipe to swappy
bind = ,Print, exec, grim -g "$(slurp)" - | swappy -f -
# Screenshot a window and pipe to swappy
bind = CTRL, Print, exec, .config/hypr/scripts/screenshot_window.sh
# Screenshot the current display and pipe to swappy
bind = SHIFT, Print, exec, .config/hypr/scripts/screenshot_display.sh

# https://github.com/hyprwm/contrib/blob/main/grimblast/grimblast.1.scd
# Print: All outputs
# SHIFT+Print: Select area
# $mainMod+Print: Current window
# $mainMod+Shfit+Print: Current output
#
# bind = ,Print, exec, grimblast save screen && notify-send Screenshot captured
# bind = SHIFT, Print, exec, grimblast save area && notify-send Selected\ area captured
# bind = $mainMod, Print, exec, grimblast save active && notify-send Active\ window captured
# bind = $mainMod SHIFT, Print, exec, grimblast output active && notify-send Output captured

# for resizing window
# will switch to a submap called resize
bind=$mainMod,R,submap,resize

# will start a submap called "resize"
submap=resize

# sets repeatable binds for resizing the active window
binde=,right,resizeactive,50 0
binde=,L,resizeactive,50 0
binde=,left,resizeactive,-50 0
binde=,H,resizeactive,-50 0
binde=,up,resizeactive,0 -50
binde=,K,resizeactive,0 -50
binde=,down,resizeactive,0 50
binde=,J,resizeactive,0 50

# use reset to go back to the global submap
bind=,escape,submap,reset

# will reset the submap, meaning end the current one and return to the global one
submap=reset

# to move window
bind = $mainMod ALT,up, movewindow, u
bind = $mainMod ALT,K, movewindow, u
bind = $mainMod ALT,down, movewindow, d
bind = $mainMod ALT,J, movewindow, d
bind = $mainMod ALT,left, movewindow, l
bind = $mainMod ALT,H, movewindow, l
bind = $mainMod ALT,right, movewindow, r
bind = $mainMod ALT,L, movewindow, r

# special workspace

bind = $mainMod SHIFT, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, +0
bind = $mainMod SHIFT, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod SHIFT, S, togglespecialworkspace, magic

# other blurings
blurls = wofi
blurls = walker
blurls = thunar
blurls = gedit
blurls = gtk-layer-shell # for nwg-drawer
blurls = catfish
blurls = Enpass
blurls = nwg-displays

# window rules

# window rules with evaluation
windowrulev2 = opacity 0.95 0.95, floating:1

exec-once = mako
exec-once =/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = $terminal
exec-once = ~/.config/hypr/scripts/restart_elephant.sh
exec-once = walker --gapplication-service

# experimental(might work might won't)

# pre executions (under development)
exec-once=exec xrdb -load ~/.Xresources

# Clipboard Manager
bind = $mainMod, s, sendshortcut, CTRL, s,  # Save
bind = SUPER, menu, exec, walker --provider clipboard
bind = SUPER, grave, exec, walker --provider windows
bind = SUPER, period, exec, walker --provider menus:ai
bind = SUPER, V, exec, $terminal --class=com.example.clipse --title=clipse --background-opacity=1 --adjust-cell-height=-15% -e clipse
bind = CTRL, menu, exec, copyq show

# video play paues bindings
bind=,172,exec,playerctl play-pause
bind=,173,exec,playerctl previous
bind=,171,exec,playerctl next

# Use gtk-settings
exec-once = apply-gsettings


# Performance Tweaks (For less power consumption without sacrificing performance)
# See : https://wiki.hyprland.org/Configuring/Performance/
misc:vfr = true

# enable G-Hyprland
bind = $mainMod SHIFT, G, exec, footclient -e ~/.local/bin/bear/implement_gum.sh enable
# Install Garuda Hyprland
# bind = $mainMod, I, exec, .local/bin/calamares.sh

# some env variables
env = XDG_CURRENT_DESKTOP,Hyprland
# https://www.hyprflux.dev/hyprland/envariables.html#development-setup
env = ELECTRON_OZONE_PLATFORM_HINT,wayland
env = QT_QPA_PLATFORM,qt6ct:wayland
env = QT_QPA_PLATFORMTHEME,qt5ct:qt6ct
# we usually prefer things to be set up through script
# have a look at hyprstart file

####################### FIXME sync via comment
#  hyprctl reload
# ok
# ❯ hyprpm update
# [sudo] password for voznik:
# → The hyprpm state store doesn't exist. Creating now...
# 
# ! Cloning https://github.com/hyprwm/Hyprland, this might take a moment.
# ✔ Hyprland cloned
# ✔ checked out to running ver
# ! configuring Hyprland
# ✔ configured Hyprland
# ✔ installed headers
#  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  5 / 5  Done!
# ✖ No repos to update.
# ✔ Ensuring plugin load state
# ✔ Plugin load state ensured
# ❯ hyprpm add https://github.com/hyprwm/hyprland-plugins
# [sudo] password for voznik:
# 
# ✔ Headers up to date.
# ! Disclaimer:
# plugins, especially not official, have no guarantee of stability, availability or security.
# Run them at your own risk.
# This message will not appear again.
# ✔ adding a new plugin repository from https://github.com/hyprwm/hyprland-plugins
#   MAKE SURE that you trust the authors. DO NOT install random plugins without verifying the code and author.
#   Are you sure? [Y/n] y
# 
# → Cloning https://github.com/hyprwm/hyprland-plugins
# ✔ cloned
# ✔ found hyprpm manifest
# ✔ parsed manifest, found 9 plugins:
# → borders-plus-plus by Vaxry version
# → csgo-vulkan-fix by Vaxry version
# → hyprbars by Vaxry version
# → hyprexpo by Vaxry version
# → hyprfocus by Vaxry version
# → hyprscrolling by Vaxry version
# → hyprtrails by Vaxry version
# → hyprwinwrap by Vaxry version
# → xtra-dispatchers by Vaxry version
# → Manifest has 38 pins, checking
# ✔ commit pin 64b7c2dff7e5e1fcb4cb7e5db078947744070e1a matched hl, resetting
# ✔ Hyprland headers OK
# → Building borders-plus-plus
# ✔ built borders-plus-plus into borders-plus-plus/borders-plus-plus.so
# → Building csgo-vulkan-fix
# ✔ built csgo-vulkan-fix into csgo-vulkan-fix/csgo-vulkan-fix.so
# → Building hyprbars
# ✔ built hyprbars into hyprbars/hyprbars.so
# → Building hyprexpo
# ✔ built hyprexpo into hyprexpo/hyprexpo.so
# → Building hyprfocus
# ✔ built hyprfocus into hyprfocus/hyprfocus.so
# → Building hyprscrolling
# ✔ built hyprscrolling into hyprscrolling/hyprscrolling.so
# → Building hyprtrails
# ✔ built hyprtrails into hyprtrails/hyprtrails.so
# → Building hyprwinwrap
# ✔ built hyprwinwrap into hyprwinwrap/hyprwinwrap.so
# → Building xtra-dispatchers
# ✔ built xtra-dispatchers into xtra-dispatchers/xtra-dispatchers.so
# ✔ all plugins built
# ✔ installed repository
# ✔ you can now enable the plugin(s) with hyprpm enable
#  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  5 / 5  Done!
# ❯ hyprpm enable hyprexpo
# [sudo] password for voznik:
# ✔ Enabled hyprexpo
# ✔ Ensuring plugin load state
# ✔ Loaded hyprexpo
# ✔ Plugin load state ensured
